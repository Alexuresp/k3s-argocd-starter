apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage-dashboard
spec:
  parentRefs:
    - name: gateway-internal
      namespace: gateway
  hostnames:
  - "homepage.192.168.1.18.traefik.me"
  rules:
  - matches:
    - path:
        type: PathPrefix 
        value: /
    backendRefs:
    - name: homepage-dashboard
      port: 3000
