apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: searxng
  namespace: searxng
spec:
  parentRefs:
    - name: gateway-external
      namespace: gateway
      sectionName: http
    - name: gateway-external
      namespace: gateway
      sectionName: https
  hostnames:
    - "search.192.168.1.19.traefik.me"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: searxng
          port: 8080
