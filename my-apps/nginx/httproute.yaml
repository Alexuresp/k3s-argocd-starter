# File: nginx/httproute.yaml
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx
  namespace: nginx-example
spec:
  parentRefs:
    - name: gateway-internal
      namespace: gateway
      sectionName: http
    - name: gateway-internal
      namespace: gateway
      sectionName: https
  hostnames:
    - "nginx.192.168.1.18.traefik.me"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: nginx
          port: 80
